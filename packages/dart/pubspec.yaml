name: healthspec
description: Type system and platform mapping for the HealthSpec specification — HealthKit and Health Connect described in one place.
version: 0.1.0
homepage: https://github.com/dev-eyoungmin/healthspec
repository: https://github.com/dev-eyoungmin/healthspec

environment:
  sdk: ">=3.0.0 <4.0.0"

dev_dependencies:
  test: ^1.25.0
