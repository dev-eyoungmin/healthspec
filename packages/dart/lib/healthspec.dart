/// HealthSpec — the specification's type system for Dart and Flutter.
///
/// Carries no implementation: it describes which health types exist, what each platform calls them, and the
/// canonical units the specification is written in. A Flutter plugin implements the behaviour on top.
library;

export 'src/generated/healthspec.dart';
