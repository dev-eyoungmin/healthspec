{
  "name": "@healthspec/verify",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "Cross-checks the spec's platform mappings against authoritative external sources (header-generated files, vendor SDK sources).",
  "scripts": { "typecheck": "tsc -p tsconfig.json" }
}
