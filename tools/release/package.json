{
  "name": "@healthspec/release",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "Checks the npm tarballs before publishing: entry points, native sources and resolved workspace versions.",
  "scripts": {
    "check": "tsx src/check-packages.ts",
    "typecheck": "tsc -p tsconfig.json"
  }
}
