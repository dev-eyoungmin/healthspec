{
  "name": "healthspec-example",
  "version": "0.0.0",
  "private": true,
  "main": "index.ts",
  "scripts": {
    "start": "expo start",
    "ios": "expo run:ios",
    "android": "expo run:android",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "node --import tsx --test test/*.test.ts"
  },
  "dependencies": {
    "@healthspec/core": "workspace:*",
    "@healthspec/expo": "workspace:*",
    "@healthspec/schema": "workspace:*",
    "expo": "^57.0.22",
    "expo-status-bar": "^57.0.1",
    "react": "^19.3.0",
    "react-native": "^0.87.1"
  },
  "devDependencies": {
    "@types/react": "^19.3.0",
    "typescript": "^5.9.0"
  }
}
